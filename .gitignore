.DS_Store

# Operator PII ruleset for gitleaks is installed at a fixed path by the
# private blueprint — no longer symlinked into this checkout, so there is
# nothing under that name for this file to ignore.
